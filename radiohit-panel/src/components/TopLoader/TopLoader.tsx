"use client";
import NextTopLoader from "@/components/TopLoader/TopLoaderHelper";

const TopLoader = () => {
    return (
        <NextTopLoader height={3} shadow={false} color="#d946ef" showSpinner={false} />
    );
};

export default TopLoader;
